package com.coffeemapper;

public class RepositoryBase<T> {
    // Get (all)
    // Get

    /*
     * public Task<IEnumerable<TEntity>> GetAllAsync() =>
     * _dataContext.GetAllAsync();
     * 
     * public Task<TEntity> GetSingleAsync(string parameterName, object
     * parameterValue) => _dataContext.GetSingleAsync(parameterName,
     * parameterValue);
     * 
     * public Task<TEntity> GetSingleAsync(long id) =>
     * _dataContext.GetSingleByIdAsync(id);
     * 
     * public Task<long> AddAsync(TEntity item) => _dataContext.AddAsync(item);
     * 
     * public Task<long> UpdateAsync(TEntity item) =>
     * _dataContext.UpdateAsync(item);
     * 
     * public Task<long> DeleteAsync(TEntity item) =>
     * _dataContext.DeleteAsync(item);
     */
}
